import 'package:flutter/material.dart';

class AppSizes {
  //--- BorderRadius
  static final brM = BorderRadius.circular(16);
  static final brL = BorderRadius.circular(20);
  //--- Radius
  static const rL = Radius.circular(20);
  static const rXl = Radius.circular(25);
}
